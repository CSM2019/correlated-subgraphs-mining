# correlated-subgraphs-mining 

The repository contains the binaries for performing correlated subgraphs mining. To execute, simply run: 
```c++
./csm <data-file> <min-sup> <hop> <k> <output-file> <control> //(supply control=1)  
```
